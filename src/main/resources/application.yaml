server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dev_user_db
    username: useradmin
    password: uadmin123
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
